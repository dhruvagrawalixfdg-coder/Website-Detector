{
  "name": "ad-web-detect",
  "version": "1.0.0",
  "description": "AI-Powered Phishing Website Detection System — Full Stack (React + Node.js + Python)",
  "scripts": {
    "backend": "node backend/server.js",
    "frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix backend install && npm --prefix frontend install && npm --prefix frontend run build",
    "start": "node backend/server.js",
    "dev": "concurrently --kill-others-on-fail --names \"BACKEND,FRONTEND\" --prefix-colors \"cyan,magenta\" \"npm run backend\" \"npm run frontend\"",
    "render-build": "npm install && npm --prefix backend install && npm --prefix frontend install && npm --prefix frontend run build",
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install"
  },
  "keywords": [
    "phishing",
    "detection",
    "security",
    "AI"
  ],
  "author": "PhishGuard AI",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
